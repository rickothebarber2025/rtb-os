#include "app_common/app_common/zap_generated/callback.h"

using namespace chip;

{%- for name in clusters | sort(attribute='name') | map(attribute='name') | unique %}
void __attribute__((weak)) emberAf{{name}}ClusterInitCallback(EndpointId endpoint)
{
    // To prevent warning
    (void) endpoint;
}
void __attribute__((weak)) emberAf{{name}}ClusterShutdownCallback(EndpointId endpoint)
{
    // To prevent warning
    (void) endpoint;
}
{%- endfor %}
